# AI Project Memory

This folder is shared by OpenClaw agents on macOS and Hermes inside Docker.

Path mapping:
- macOS/OpenClaw: /Users/daniel/Projects/<repo>
- Hermes Docker: /workspace/<repo>

Rules:
- Read project-context.md before making changes.
- Check active-sprint.md and backlog.md before proposing work.
- Use handoffs/current-handoff.md when passing work between agents.
- Use locks/ to avoid multiple agents editing the same files.
