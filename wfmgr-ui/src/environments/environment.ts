// Production environment placeholder.
// Update apiBaseUrl to the deployed API URL before running a production build.
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:5000'
};
