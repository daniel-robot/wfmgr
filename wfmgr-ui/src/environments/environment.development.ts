// Development environment — matches the API port in Wfmgr.Api/Properties/launchSettings.json.
// Update apiBaseUrl if you change the API port or run it on a different host.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5225'
};
